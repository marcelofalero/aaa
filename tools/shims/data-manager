#!/bin/bash
# Shim for data-manager — Unified YAML ↔ Markdown review tool
PROJECT_ROOT="@PROJECT_ROOT@"
cd "$PROJECT_ROOT" || { echo "Error: project root not found at $PROJECT_ROOT" >&2; exit 1; }
exec pipenv run python3 tools/data_manager.py "$@"
