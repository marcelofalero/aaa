#!/bin/bash
# Shim for manage-site-data — Generate Hugo content + JSON from YAML data sources
PROJECT_ROOT="@PROJECT_ROOT@"
cd "$PROJECT_ROOT" || { echo "Error: project root not found at $PROJECT_ROOT" >&2; exit 1; }
exec pipenv run python3 tools/scripts/manage_site_data.py "$@"
