#!/bin/bash
# Shim for generate-legacy-skills — Generate legacy skills JSON for Roll20 macros
PROJECT_ROOT="@PROJECT_ROOT@"
cd "$PROJECT_ROOT" || { echo "Error: project root not found at $PROJECT_ROOT" >&2; exit 1; }
exec pipenv run python3 tools/scripts/generate_legacy_skills_json.py "$@"
