#!/bin/bash
# Shim for check-search-index — Verify search index has key content before publishing
PROJECT_ROOT="@PROJECT_ROOT@"
cd "$PROJECT_ROOT" || { echo "Error: project root not found at $PROJECT_ROOT" >&2; exit 1; }
exec pipenv run python3 tools/scripts/check_search_index.py "$@"
